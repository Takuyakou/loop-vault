# P5.18.1-01 — Preset Domain / Catalog

## Determination

`GATES PASS — preset domain/catalog complete; awaiting explicit P5.18.1-02 authorization`

## Scope

Implemented only the P5.18.1 preset-domain/catalog boundary and the authorized
Contract 06 section-length extension. No picker or Bassline Echo source-selector
UI was started.

## Catalog

- Version: `bassline-preset-catalog-v1`
- Nine immutable, data-driven entries: `legacy-generated-default`,
  `fourth-fifth-foundation`, `pop-four-chords`, `minor-descent`, `turnaround`,
  `ii-v-i`, `modal-rock`, `descending-bass`, and `twelve-bar-blues`.
- Each entry contains its formula, category, skill tags, 4/4 meter, default BPM,
  and deterministic major/minor tonal-centre policy.
- Borrowed degrees and slash bass are represented in formulas; no chord identity
  is substituted.

## Compatibility

- The existing generated Dm7–G7–Cmaj7 default remains unchanged and valid.
- It now has a distinct, explicit legacy-compatible catalog identity; no prior
  generated or Vault snapshot is rewritten.
- Existing one- and two-bar snapshots remain supported.
- Chord Context factual History can represent the detached `preset` identity
  (catalog version, preset ID, safe label, and section facts only).

## Transposition / sections

- Deterministic transposition was verified for every catalog form in all twelve
  supported tonal centres of its original major/minor mode.
- Contract 06 limits source sections to exact complete 4/4 lengths of 4, 8, 16,
  32, or 48 beats. No arbitrary duration was introduced.
- The bounded maximum is 48 beats and 48 chord-onset events.
- A complete 12-bar plan was verified for deterministic scheduling of Bass,
  Chords, and Metronome. The existing count-in remains separately bounded to
  eight beats.

## Safety / protected surfaces

- Preset snapshots are detached, immutable, canonicalized, and fail closed for
  malformed chord input.
- Vault remains read-only. No Vault schema/mutation, Analyzer, MIDI Exporter,
  Chord Dojo, Live MIDI, P5.15, FreePats asset, or P5.17 recording-store change.
- No MIDI, recording, `.local-evaluation`, or personal absolute path is tracked
  or recorded by this Stage.

## Git reconciliation

At Stage start, `execution-state.json` and Git both identified P5.18.1-01 as
active, while the README's final next-action block still named P5.18.1-00. This
was documentation drift only; the Stage 01 closure corrects that stale block.

## Gates

All results below were rerun on exact code commit
`64828ae8d71240e13ef6cf61a11360a2f6f416b7`.

- `npx tsc --noEmit`: PASS
- Focused domain/playback Vitest: 8 files / 46 tests PASS
- `npx vitest run src/features/bass-practice`: 46 files / 326 tests PASS
  (one pre-existing React `act(...)` warning only)
- `npm run lint`: PASS, 0 errors; one pre-existing unused-disable warning in an
  unchanged recording UI file
- `npm run typecheck:e2e`: PASS
- `npm run validate:phase-docs`: PASS (4 phase packages)
- `git diff --check`: PASS

## Stage commit

`64828ae8d71240e13ef6cf61a11360a2f6f416b7`
`feat(P5.18.1-01): add Bassline preset catalog`

## Next action

Stop after this documentation closure. P5.18.1-02 requires explicit human
authorization and has not been started.
