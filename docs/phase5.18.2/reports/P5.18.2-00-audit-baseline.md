<!-- phase-id: 5.18.2 -->

# P5.18.2-00 — Audit / Contract / Baseline

## Determination

`PASS — repository audit, title boundary, and baseline gates complete`

## Findings

The existing Vault title is the required `SongIdea.title` field. It is live user text and must not cross the existing P5.18.1 detached snapshot boundary. The current picker intentionally exposes only safe harmonic facts, so Stage 01 can add a presentation-only ViewModel without changing the Vault schema, mutation behavior, snapshot, History, or recording contracts.

Git reality at entry was master `81a6890` with clean tracked content and an untracked P5.18.2 phase documentation package. The package was preserved on the dedicated branch and normalized to the repository phase-document schema. P5.18.1 and test-output hygiene are ancestors; P5.15 is not.

## Title boundary

- Allowed: live picker display and picker-local search.
- Forbidden: `VaultChordContextSnapshot`, Practice persistence, Chord Context History, retained-take metadata, reports, logs, telemetry, raw MIDI, source file paths, and full Vault objects.
- Fallback: localized neutral untitled text in presentation only.
- Search: title augments the existing safe harmonic search; Unicode is preserved and duplicate titles remain distinguishable by stable candidate identity and secondary facts.

## Progression Detail determination

`exists-and-works` — the existing Detail action selects a safe section, passes an immutable snapshot into the established `openPractice(snapshot)` handoff, and fails closed for unavailable sources. No route or source-model replacement is authorized.

## Test-output hygiene baseline

The `81a6890` hygiene contract is protected: normal Playwright output is ignored, tracked visual evidence is read-only, baseline updates require the explicit update-baselines command, and Cargo.toml uses LF normalization. Full Playwright and Tauri verification are deferred to the final Stage 03 release gate.

## Gates

- Phase docs validator: PASS
- Validator tests: PASS — 1 file / 25 tests
- Focused baseline tests: PASS — 6 files / 46 tests
- Combined focused baseline command: PASS — 7 files / 71 tests
- Bass Practice regression: PASS — 47 files / 334 tests
- Lint: PASS — 0 warnings
- App and E2E typechecks: PASS
- `git diff --check`: PASS

## Stage commit

Stage audit and contract commit: `bd656624025724ba46fb6bb7a5640323c92dcf8d`.

## Next action

P5.18.2-01 is not started. Await separate authorization before implementation.
