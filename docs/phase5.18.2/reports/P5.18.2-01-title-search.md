<!-- phase-id: 5.18.2 -->
# P5.18.2-01 — Live Title ViewModel / Search

## Determination
PASS — live Vault title is isolated to picker candidate presentation and search.

## Branch and code commit
- Branch: `feat/p5182-vault-source-discoverability`
- Base: `81a689088436015f16596fa757998926b017718a`
- Stage code commit: `0392223e947784298c01d75b2497f2ff76bbbf44`
- Master merge: not performed
- Push: not performed

## Scope delivered
- Added an immutable picker-only `VaultPickerCandidateView` with `displayTitle`, `searchableTitle`, and the existing safe snapshot.
- Builds candidates afresh from the current live Vault only while Bass Practice is active.
- Normalizes Unicode and whitespace for display/search; uses a neutral localized fallback for missing or whitespace-only titles.
- Preserves duplicate and long titles as separate candidates by their stable safe source snapshots.
- Adds title matching without removing existing key, section, or chord matching.
- Preserves the existing confirmation transaction: only the safe snapshot signature crosses into Bassline practice.

## Privacy boundary evidence
- Live title is absent from `VaultChordContextSnapshot`, Chord Context History, and recording metadata paths.
- The candidate ViewModel is transient App/UI data only; it is not written to Vault, Practice JSON, History, reports, logs, or telemetry.
- Negative serialization tests cover safe snapshots and a factual History entry derived from a candidate snapshot.
- No Vault schema, Vault mutation, Analyzer, MIDI Exporter, P5.15, Chord Dojo, Live MIDI, FreePats, or recording-store path changed.

## Search behavior
- Case-insensitive matching where applicable.
- Unicode normalization and trim handling.
- Title, key, bar range, and chord-label matching.
- Empty/whitespace fallback, duplicate, long, symbol, and Unicode title coverage.
- Stage 02 owns the title-first visible picker row and accessibility/viewport presentation work; Stage 01 does not change that layout.

## Gates on code commit
| Gate | Result |
| --- | --- |
| ViewModel / picker / privacy / App / Progression Detail focused tests | PASS — 8 files / 47 tests |
| Bass Practice plus App / Progression Detail regression | PASS — 50 files / 347 tests |
| App TypeScript | PASS |
| E2E TypeScript | PASS |
| Lint and Tailwind class lint | PASS |
| Phase docs validation | PASS |
| `git diff --check` | PASS |
| Protected-path, tracked MIDI / `.local-evaluation`, personal-path scans | PASS |

## Test-output hygiene
Full Playwright and Tauri release build are Stage 03 release gates and were not run for Stage 01. No generated output or build artifact was created or staged.

## Git status
Clean after the code commit and exact-HEAD Gate run. This report and execution-state update are committed separately as docs-only closure work.

## Next action
Do not start P5.18.2-02 without separate human authorization. Do not merge or push.
