<!-- phase-id: 5.19 -->
# P5.19-00 Repository Audit

## Determination
`AUDIT COMPLETE — contract lock and baseline verification pending the audit-document commit`

## Git and worktree reality

- Dedicated branch: `feat/p519-root-motion-echo`.
- Audit base and local `master`: `854a5c909660e231dfafcc184c3881e99799911e`.
- The local default branch is ahead of `origin/master` (`81a689088436015f16596fa757998926b017718a`). No rebase, reset, merge, or push was performed.
- The P5.18.2 merge is an ancestor of the base. The P5.18, P5.18.1, and test-output-hygiene integration ancestors are also present.
- P5.15 commits `b186281`, `41f50c0`, and `97b03f4` are not ancestors of the base.
- No merge, rebase, or cherry-pick is in progress. `docs/CURRENT_STATE.md` is absent.
- Existing unrelated prunable worktree records were observed and left untouched.
- Git reality discrepancy: the human-supplied P5.19 document package was absent from local `master` and untracked in the prior source worktree. It was copied unchanged into this clean P5.19 worktree before this audit; no production source was copied or changed.

## Existing Practice feasibility

- Degree and Rhythm retain their legacy domain types. `PracticeExercise` and `PracticeAttempt` remain Degree-specific and must not be widened for Root Motion.
- The persisted Practice envelope is strict `fileVersion: 1` and already supports additive optional/defaulted collections (Rhythm and Chord Context history). An optional/defaulted Root Motion history collection is feasible without a Vault schema change or a file-version bump.
- Existing degree state, generator, hints, transfer, and review queue are reusable references, not a domain to mutate in Stage00.
- Existing Bass Practice settings support four/five strings, handedness, and bounded fret ranges. Standard tuning MIDI roots are four `[28, 33, 38, 43]` and five `[23, 28, 33, 38, 43]`.
- `fretboardPositions` enumerates legal pitch positions but no canonical source/target pair solver exists. P5.19 therefore needs a separate deterministic solver.
- The current fretboard renderer supports left/right visual orientation. Root Motion semantic labels must remain physical and handedness-independent.

## Playback, recording, and Vault feasibility

- Existing FreePats/Tone playback and P5.17 Record & Compare expose the required stop/dispose lifecycle. P5.19 must reuse these contracts and add no recorder/store.
- Existing Chord Context snapshots are safe detached values and represent chord-root pitch classes. They intentionally do not preserve original bassline data, titles, or octave direction.
- P5.18.2 picker titles are UI-only. Root Motion source and History must use safe snapshot/reference facts rather than titles.
- Deterministic root-path policy v1, canonical fingering ranks, exact motion vocabulary, first-answer evidence, and additive factual History are fixed in contracts 02–05.

## Stage boundary

P5.19-00 adds documentation and baseline evidence only. It does not start P5.19-01, alter production behavior, modify the Vault schema, or touch protected surfaces.