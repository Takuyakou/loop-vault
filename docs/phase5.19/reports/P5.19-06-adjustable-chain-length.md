# P5.19-06 — Adjustable Root Motion Chain Length

## Determination

`IN PROGRESS — contract locked for selectable 2..8-note Root Motion phrases`

The user approved a post-acceptance P5.19 scope amendment after confirming the
Transfer persistence hotfix. This stage extends phrase length only; it keeps
first-transition objective evidence and self-review semantics intact.

## Fixed decisions

- UI setting: two through eight notes, default two, persisted additively in
  Practice settings with legacy fallback.
- Level determines first-transition answer complexity, not phrase length.
- Two beats per note yields 4..16-beat phrases.
- Vault sections shorter than the selected count are unavailable without
  truncation or source substitution.
- Transfer preserves the full signed sequence; History permits up to seven
  motions and remains backwards compatible.
- Longer fretboard phrases use ordered markers rather than collapsing all
  intermediate positions into source/target labels.

## Protected surfaces

No P5.15, Vault schema/mutation, Analyzer, MIDI Exporter, external MIDI,
recording, raw audio, personal path, or `.local-evaluation` changes are in
scope.

## Gates

No implementation gate has run yet. The P5.19-06 contract requires the focused
domain, Vault, persistence, UI/accessibility, typecheck, lint, production build,
and protected-surface gates recorded in `execution-state.json`.

## Next action

Implement the selectable 2..8-note setting and its deterministic, persistent,
Vault-safe, and history-compatible behavior. Do not merge, push, or start P5.20.