# References

`P5.20-UI-UX-MOCK.html` is a non-production interaction reference. The mock parser is not production code. Production must reuse the repository's actual chord parser, canonical identity, ProgressionGrid/card system and CaptureDraft.
