<!-- phase-id: 5.20 -->
# P5.20-00 — Audit / Grammar / Contract / Baseline

## Determination

`PASS — P5.20-00 audit, grammar/contract lock, and baseline`

## Verified code commit

`6b28bbb9886f876f938881b1af91c80f79ae6982` on
`feat/p520-text-progression-entry`.

This is a documentation-only audit-and-contract commit. No production behavior
changed. The closure documentation below records fresh Gates run against that
exact HEAD.

## Audit and contract lock

- The P5.20 package was supplied untracked in a separate dirty worktree. It was
  copied without touching that worktree, then committed only in the dedicated
  clean Phase branch. Git reality found merged P5.19 but no P5.19.1 tracked
  phase/commit; the false P5.19.1 prerequisite was corrected in this package.
- Text is a Capture-local input mode and becomes the existing session-only
  `ManualCandidateDraft` through a one-way conversion. No new route, persistent
  progression type, fake MIDI analysis, source asset, source path, or file
  provenance is allowed.
- Grammar v1 is bounded 4/4: bar/simple notation, exact 1/2/4 chord bars,
  canonical absolute labels, confirmed-key-only Roman/numeric input, source
  ranges and diagnostics. `N.C.`, empty/three-chord bars, repeats, comments,
  and unsupported syntax block conversion without approximation.
- Key is transient `unknown` / `inferred` / `confirmed`; only confirmed key can
  parse Roman input, display degrees, or become persisted `detectedKey`.
- Auto Voicing is the existing deterministic per-chord generator and must be
  labelled Auto/Generated. Custom Voicing is **AUTHORIZED** solely through the
  existing Live MIDI `practiceVoicingOverride` path; exact notes already fit the
  strict fileVersion 1 schema, so no storage/migration/click-editor work is
  approved.
- Capability is separate per consumer. Vault Save remains valid when practice
  needs confirmed key, BPM 30–240, 4/4, a complete contiguous 1/2/4/8/12-bar
  section, and at most 48 events.

## Gate results

| Gate | Result |
| --- | --- |
| Phase docs validation | PASS — 7 phase packages |
| Phase-docs validator | PASS — 1 file / 25 tests |
| Parser / Draft / Voicing baseline | PASS — 8 files / 82 tests |
| Capture / Vault regression | PASS — 4 files / 78 tests |
| Bass Practice regression | PASS — 55 files / 395 tests |
| Lint and class lint | PASS |
| App TypeScript | PASS |
| E2E TypeScript | PASS |
| Rust | PASS — 41 tests |
| `git diff --check` | PASS |
| Protected and privacy surfaces | PASS — all required counts 0 |

## Protected surfaces

The branch range contains only P5.20 documentation. There are no changes to
P5.15, Vault/Practice schemas, Analyzer, MIDI Exporter, Live MIDI, FreePats,
P5.17 RecordingTake storage, test-output hygiene, or app production code.
Tracked MIDI and `.local-evaluation` counts are zero; P5.20 documents contain
no personal absolute paths.

## Stage commits

- Audit/contract document commit: `6b28bbb9886f876f938881b1af91c80f79ae6982`.
- This closure commit records the exact-HEAD Gate results and is documentation-only.

## Next action

P5.20-00 is complete. Do not start P5.20-01 without explicit human
authorization. Do not merge or push.
