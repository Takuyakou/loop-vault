# P5.20-01 — Parser / Diagnostics / Capability Evaluator

## Determination

`PASS`

Code commit: `ac3ac1310ecb1669de06981a0412c9f846c32979`

## Parser

- Added a bounded, text-only Grammar v1 parser in `src/domain/textProgression.ts`.
- Supports exact 4/4 bar notation (one, two, or four chords per bar) and simple whitespace notation (one four-beat bar per chord).
- Canonicalizes through existing `parseChordLabel()` / `labelFromSymbol()`; slash chords, including `C6/9`, remain one token.
- Roman or numeric input reaches existing `parseFastChordEntry()` only after a valid, explicit key confirmation. It never fabricates MIDI analysis or analyzer evidence.

## Diagnostics

- Each valid/invalid visible token retains its raw form, canonical form when valid, UTF-16 range, bar, and exact timing.
- The 4,096-code-unit, 12-bar, and 48-token bounds fail closed with a diagnostic; no truncated subset can become a Draft.
- Unsupported rests, empty bars, three-chord bars, repeat/comment/header syntax, malformed tokens, whitespace-containing headers, and lyric text are reported explicitly.
- Deterministic text-only key candidates are transient suggestions. Only a confirmed key can enable Roman input or later be persisted.

## Timing

- One, two, and four chord bars allocate exact 4, 2, and 1 beat durations.
- The parser does not approximate triplets, unsupported meters, gaps, or partial sections.

## Capability

- Capability results remain independent for Vault Save, Chord Dojo, Bass Practice, Chord Context, Root Motion, and Voicing Memory.
- Chord Context evaluates actual selectable complete 1/2/4/8/12-bar sections, so a valid three-bar card can correctly expose an eligible one-bar section.
- Root Motion is `unknown` until a 2–8 note count is selected, then checks that an eligible selectable section has enough roots. Text is never labelled as an original bassline.

## Tests

- Parser / property-fuzz / capability: 17 tests PASS.
- Parser plus existing chord, Fast Label, Chord Context, and Root Motion contracts: 6 files / 65 tests PASS.
- App TypeScript, focused ESLint, and `git diff --check`: PASS.
- Protected-surface and personal-path scan: PASS. No MIDI, recordings, `.local-evaluation`, Vault schema, Practice schema, Analyzer, MIDI Exporter, or P5.15 paths changed.

## Stage commit

`ac3ac1310ecb1669de06981a0412c9f846c32979`

## Next action

P5.20-02 may add the Capture text UI and one-way Draft bridge. It must not change this parser contract or introduce synthetic MIDI/source metadata.
