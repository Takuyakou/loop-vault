# P5.21.1 Backlog — Mixed Voice Note Filtering
Status: `DEFERRED — explicitly out of P5.21`

Voice内でharmony+melodyが混在する問題はVoice classificationからnote classificationへ粒度が変わるため別Phase。P5.21後のresidual failuresを見て必要性を再判断する。top-voice suppression/per-note weight/note-level role score等の実装はこの文書では許可しない。
