# P5.21-00 — Audit / Current Baseline / Approved Role Ground Truth

## Determination

`PASS — P5.21-00 baseline and promotion lock complete`

P5.21-00 is complete at code candidate `252ac2b1d9a16eacf7cef1541e782b127d82e8f3` on `feat/p521-harmonic-core-role-v2`.

- The supplied deterministic synthetic pack provides the approved fixture-defined Voice ground truth; current Role v1 predictions were measured separately and were never adopted as truth.
- No production role behavior, Analyzer scoring, boundaries, candidate generation, defaults, Vault schema/fileVersion, Voicing Memory, or P5.15 source changed.
- P5.21-01, P5.21.1 mixed-Voice note filtering, P5.22, merge, and push remain unstarted.

## Git reality and reconciliation

At resumption, the phase documents correctly recorded P5.21-00 as blocked because the earlier local registry had 0 approved expected roles. The supplied fixture pack removed that specific input blocker. Git reality is authoritative:

- Branch: `feat/p521-harmonic-core-role-v2`.
- Stage 00 code candidate: `252ac2b1d9a16eacf7cef1541e782b127d82e8f3` (`chore(P5.21-00): import approved synthetic role fixtures`).
- P5.15 commits `b186281`, `41f50c0`, and `97b03f4` are not ancestors of the candidate.
- `docs/CURRENT_STATE.md` is absent.
- No merge, rebase, cherry-pick, or push was performed.

## Approved synthetic ground truth and privacy

The user-supplied local fixture pack was validated without emitting its raw contents:

- Fixture manifest schema: `p5.21-generated-fixture-manifest-v1`.
- Ground-truth schema: `p5.21-role-ground-truth-v1`.
- 8 fixtures, 26 Voice labels, and 8 of 8 MIDI SHA-256 values matched.
- 25 Voices are evaluable; 1 `ambiguous` Voice is retained for diagnostics and excluded from accuracy, role precision/recall, and correction burden.
- The importer transforms supplied fixture identities into deterministic anonymous IDs before scanning. It writes only ignored local artifacts: an approved role registry, a metrics JSON, and an HTML evidence view. No source paths, raw MIDI, titles, raw notes, recordings, or device identifiers are copied into tracked files or this report.
- `expectedRole` comes only from fixture-defined ground truth. Current classifier output is diagnostic measurement, not a truth fallback.

Local regeneration command, after the approved pack is present in its ignored P5.21 area:

```text
npx vite-node scripts/p521/importSuppliedRoleFixturePack.ts --run-cli
```

It produces the ignored local registry, metric summary, and browser-openable evidence view under `.local-evaluation/p521-role-ground-truth/supplied-fixture-baseline/`.

## Current production Role v1 baseline

| Metric | Locked baseline |
| --- | ---: |
| Total Voices | 26 |
| Evaluated Voices | 25 |
| Ambiguous Voices | 1 |
| Exact role accuracy | 80.0% |
| Manual correction count | 5 |
| Manual correction burden | 20.0% |
| Mixed prediction rate | 12.0% |
| Melody recall | 60.0% |
| Harmony precision | 100.0% |
| Bass precision | 100.0% |
| Percussion precision | 100.0% |

The full confusion matrix is retained only in the ignored local baseline JSON. It contains no private MIDI material.

## Official chord-safety baseline

The current official evaluator ran against the existing schema-valid local clean/dirty ChordDrip corpora with report-only output so its current measurement could be locked without altering production behavior.

| Clean current Voice-aware metric | Locked baseline |
| --- | ---: |
| Cases | 100 |
| Root@1 | 58.1897% |
| Quality@1 | 61.0453% |
| Exact@1 | 13.6853% |
| Boundary precision | 76.5475% |
| Boundary recall | 90.0864% |
| Determinism | PASS |

The evaluator's own historical strict-guard status was `failed-strict-guard` (19 historical guard failures). This is recorded verbatim as a baseline fact; it is not recast as a passing historical regression result. P5.21-00 passes because the current-head measurement, its inputs, and the promotion comparison thresholds are now locked. A later Stage 02 promotion must meet the locked P5.21 safety rules rather than relying on this historical strict status.

## Promotion gate lock

Before Role v2 work, promotion requires all of the following against these Stage 00 baselines:

1. Manual correction burden is strictly below 20.0%, except a zero baseline may remain zero.
2. Exact role accuracy is at least 80.0%; melody recall is at least 60.0%; harmony, bass, and percussion precision are each at least 100.0%.
3. Mixed prediction rate and the complete confusion matrix are always recorded; neither alone justifies promotion.
4. Official chord safety: Root@1, Quality@1, boundary precision, and boundary recall do not regress; Exact@1 may decline by at most 0.25 percentage points.
5. Shadow output is deterministic, manual override remains authoritative, and protected Analyzer/scoring/boundary/candidate/default/Vault/Voicing surfaces remain unchanged.

A missing metric or a failed condition blocks promotion and leaves production Role v1 unchanged.

## Gates on the code candidate

| Gate | Result |
| --- | --- |
| Repository / ancestry / protected-surface audit | PASS |
| Approved synthetic fixture integrity and role import | PASS — 8 fixtures, 26 Voice labels, 8 of 8 checksums |
| Current Role v1 baseline | PASS — 25 evaluated Voices; fixture-defined ground truth |
| Official current-head chord baseline | PASS — measured and locked; historical strict status recorded above |
| Promotion gate lock | PASS |
| Focused role / pre-analysis / support tests | PASS — 10 files / 64 tests |
| Lint and app TypeScript | PASS |
| Tracked MIDI / `.local-evaluation` | PASS — 0 / 0 |
| P5.15 ancestry / protected production surfaces | PASS — 0 / 0 |
| Diff checks | PASS |

## Stop point

P5.21-00 is complete. P5.21-01 is the next incomplete stage but has not begun and requires explicit human authorization. Do not start Role feature v2, harmonic core, or Voice-internal mixed-note filtering automatically.