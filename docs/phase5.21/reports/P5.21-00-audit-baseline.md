# P5.21-00-audit-baseline — Audit / Baseline / Ground Truth

## Determination

`BLOCKED — official current baseline and approved human role ground truth unavailable`

## Scope

This stage created the P5.21 documentation package and audited the existing
Role v1 implementation. It made no production-source changes and did not start
P5.21-01, P5.21.1, P5.22, a merge, or a push.

## Git reality

- Branch: `feat/p521-harmonic-core-role-v2`
- Base / audited HEAD: `747c70cc4ef078c0a086f641436443e134115d08`
- Local master: same commit
- P5.15 frozen commits: non-ancestors
- P5.19: complete; no tracked P5.19.1 package or commit exists
- P5.20: complete; its stale merge-pending next-action text is overridden by
  the merged Git history
- Shared-worktree phase-document discrepancy: preserved and recorded in the
  repository audit; this branch was created in a separate clean worktree

## Findings

Role v1 fuses measured, GM-program, and track-name evidence, with only channel
index 9 as a hard percussion rule. It currently falls back to `mixed` below the
`0.42` score or `0.08` margin thresholds. No current-head official corpus or
approved human Voice-role registry is available, so no historical accuracy is
claimed as a P5.21 baseline.

The exact official command, `npm run eval:midi:voice-aware`, exited `1` before
execution because the isolated worktree lacks `vite-node`; both of its ignored
manifest inputs are also absent. Existing correction logging uses a strict v1
schema and numeric confidence, so P5.21 enrichment is deferred until a later
stage can prove compatibility.

## Metrics / Gates

| Gate | Result |
| --- | --- |
| repository audit | PASS |
| official Voice baseline | BLOCKED |
| human role ground truth | BLOCKED |
| promotion-gate baseline | BLOCKED |
| phase docs validation | Pending final rerun |
| focused role tests / lint / typecheck | Not run — isolated dependencies unavailable |
| protected surface / diff audit | PASS |

## Locked policy

Ambiguous Voice labels are allowed but excluded from automatic-role accuracy,
role-specific recall/precision, and manual-correction burden. Future promotion
requires a strict reduction in manual correction burden (or zero stays zero),
non-regressing role metrics, deterministic shadow output, and official exact
chord accuracy within `-0.25` percentage points of the current locked baseline.

## Protected surfaces

P5.15, scoring, boundaries, candidate generation, defaults, Vault schema/
fileVersion, Voicing Memory, private MIDI, recordings, `.local-evaluation`,
personal paths, visual baselines, and Cargo manifest line endings are unchanged.

## Stage commit

Not created as a completed stage: the required official baseline and human
ground-truth gates are blocked.

## Next action

Supply or designate the privacy-safe approved fixture registry and current
official evaluation corpus, then resume P5.21-00 only.
