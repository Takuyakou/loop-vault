# P5.21-00 — Audit / Baseline / Ground Truth Automated Preparation

## Determination

`BLOCKED — human Voice-role review and baseline lock remain incomplete`

P5.21-00 remains blocked. This update automates local evaluation preparation
only. It did not start P5.21-01, P5.21.1, Role v2, mixed-Voice note filtering,
Harmonic Core work, a merge, or a push.

## Dependency restoration

- `npm ci` completed from the existing lockfile without changing `package.json`
  or the lockfile.
- `vite-node 3.2.4` is available in the dedicated worktree.
- The package manager reported three high-severity advisories. No audit-fix,
  dependency update, or version change was performed.

## Official evaluator contract

`npm run eval:midi:voice-aware` reads these default `ChordDripCorpusManifest`
schema-v1 inputs:

| Input | Default repository-relative path |
| --- | --- |
| Clean manifest | `docs/loop-vault-evaluation-corpus/manifest.json` |
| Local dirty manifest | `.local-evaluation/phase3.6.5-dirty/manifest.json` |

Each `midiFile` is resolved relative to its manifest. The schema requires file
identity, generation record, and chord timeline; dirty entries also carry
degradation metadata. The original defaults remain absent in this dedicated
worktree, so they were not fabricated. The setup generator instead reused the
existing schema-valid evaluation manifests found in the bounded worktree search
and created ignored rebased copies for local use.

## Automated discovery and generated registry

- Bounded locations searched: registered Git worktrees plus only their
  `.local-evaluation`, official evaluation-corpus, and artifact directories.
- MIDI discovered in those locations: 2,315.
- Usable MIDI selected from existing schema-valid manifest references: 1,200.
- Generated registry: 1,200 anonymous fixtures and 2,800 Voice rows.
- Generated clean manifest: schema v1, 100 relative MIDI references.
- Generated dirty manifest: schema v1, 1,100 relative MIDI references.
- All generated manifest references were checked to resolve locally.

The generated local review bundle is at:

```text
.local-evaluation/p521-role-ground-truth/review-f170810ac353/
```

It contains:

- `role-review.html` — browser-openable human review UI;
- `role-review-registry.json` — safe, anonymous registry with `expectedRole`
  initially `null` for every Voice;
- `clean-manifest.json` and `dirty-manifest.json` — rebased local evaluator
  inputs; and
- `role-review-source-map.private.json` — ignored local mapping required for
  later local evaluation. It is never committed or reported.

The review UI displays only anonymous fixture/Voice IDs, safe program/channel
information, note count/range/duration/polyphony, current classifier prediction
and confidence, safe evidence kinds, and a non-binding suggested role. It does
not contain paths, titles, raw MIDI, raw notes, recordings, or device IDs.

`expectedRole` values are never inferred into truth. The user must either choose
a role per Voice or explicitly accept the visible suggestions after reviewing
them. The UI downloads a reviewed registry locally; no hand-edited JSON or
manifest is needed.

## Verification

| Check | Result |
| --- | --- |
| Lockfile dependency restoration | PASS |
| `vite-node` availability | PASS — `3.2.4` |
| Focused support/role/pre-analysis tests | PASS — 4 files / 24 tests |
| Targeted and full lint | PASS |
| App TypeScript | PASS |
| Discovery and registry generation | PASS — 1,200 fixtures / 2,800 Voices |
| Generated registry privacy fields | PASS — no source path/title/raw-note fields |
| Rebased manifest resolution | PASS — all 100 clean and 1,100 dirty targets exist |
| Official baseline measurement | NOT RUN — Ground Truth is unreviewed |
| Human ground truth | BLOCKED — 0 of 2,800 rows confirmed |
| Promotion lock | BLOCKED |

## Next human action

Open `role-review.html` from the generated local bundle, inspect the proposed
roles, change only the mistakes, then explicitly accept the reviewed
suggestions and download the confirmed registry. Do not supply individual MIDI
paths, hand-write a manifest, or commit the downloaded file. After local review
is complete, resume **P5.21-00 only** to run the baseline and lock the promotion
gate.
