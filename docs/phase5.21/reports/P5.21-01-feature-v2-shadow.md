# P5.21-01 — Role Feature v2 / Shadow Extraction

## Determination

`PASS — feature-only Role v2 shadow extraction complete`

Stage 01 is complete at implementation commit
`25f708e866a66b321e00c8b89c31e07db6b000ca` on
`feat/p521-harmonic-core-role-v2`.

## Implementation

- Added a deterministic, feature-only legato proxy. It joins same-pitch
  fragments at or below a fixed 0.25-beat gap exclusively while extracting
  shadow features; it neither mutates input notes nor changes raw Analyzer
  NoteEvents, source/display MIDI, Voicing Memory, or duration scoring.
- Added aggregate, inspectable diagnostics: active-time weighted monophony and
  polyphony, robust median duration, pitch center and stable pitch-center rank,
  stepwise-motion ratio, note density, and program/name/percussion evidence.
- MIDI Channel 10 (zero-based index 9) remains the only hard percussion signal.
  Program, track-name, and soft-signature data are diagnostics only at this
  stage.
- There is no production classifier hook, candidate role, confidence, or UI
  change. Production Role v1 behavior remains unchanged.
- Voice-internal mixed-note filtering, harmonic/chord-result-dependent
  features, Role v2 promotion, and Harmonic Core are not part of this stage.

## Gates

| Gate | Result |
| --- | --- |
| Focused shadow / current role / pre-analysis tests | PASS — 3 files / 25 tests |
| App TypeScript | PASS |
| Lint | PASS |
| `git diff --check` | PASS |
| Protected-surface audit | PASS — no production hook; protected Analyzer/scoring/boundary/candidate/default/Vault/Voicing/P5.15 surfaces unchanged; tracked MIDI and `.local-evaluation` remain 0 |

## Stage commit and post-commit state

- Implementation commit: `25f708e866a66b321e00c8b89c31e07db6b000ca`
  (`feat(P5.21-01): add shadow role feature extraction`).
- The Stage 01 implementation commit contains only the shadow feature module
  and its focused test. The Stage 00 baseline and this closure documentation
  are separate commits.
- Post-implementation-commit `git status --short`: clean.

## Next action

P5.21-02 is the next active stage: evaluate a Role v2 candidate in shadow mode
against the locked P5.21-00 baseline. Keep production Role v1 unchanged until
the Stage 02 promotion gate passes. Do not start P5.21.1 or P5.22, merge, or
push.