# P5.21-02 — Role v2 / Shadow Evaluation

## Determination

`PASS — Role v2 shadow candidate meets the locked promotion gate`

Stage 02 is complete at code candidate `6468cb36bd469d234df763df7873c382757456aa`
on `feat/p521-harmonic-core-role-v2`. The candidate is authorized to enter the
separate P5.21-03 promotion stage. Production Role v1 remains unchanged by this
Stage 02 candidate.

## Findings / implementation

- The approved Stage 00 synthetic fixture registry was re-read and verified
  against its fixture-defined expected roles. Current Role v1 was measured as a
  baseline only; it was never treated as ground truth.
- Role v2 ran in deterministic shadow mode. It emits candidate role,
  confidence bucket, and evidence kinds only in ignored local evaluation
  artifacts; it does not alter production inference or manual-override
  authority.
- The candidate improved the primary manual-correction burden while preserving
  every locked role-specific metric. The complete confusion matrices are
  retained only in ignored local artifacts.

## Locked role metrics

| Metric | Role v1 baseline | Role v2 shadow | Delta |
| --- | ---: | ---: | ---: |
| Exact role accuracy | 80.0% | 88.0% | +8.0pp |
| Manual correction count | 5 | 3 | -2 |
| Manual correction burden | 20.0% | 12.0% | -8.0pp |
| Mixed prediction rate | 12.0% | 8.0% | -4.0pp |
| Melody recall | 60.0% | 100.0% | +40.0pp |
| Harmony precision | 100.0% | 100.0% | 0.0pp |
| Bass precision | 100.0% | 100.0% | 0.0pp |
| Percussion precision | 100.0% | 100.0% | 0.0pp |

The registry contains 26 Voices: 25 evaluated and 1 `ambiguous` diagnostic
Voice. The ambiguous Voice is excluded from accuracy, role-specific
precision/recall, and correction-burden calculations as locked in Stage 00.

## Fresh official chord-safety attestation

- `PASS` — the official voice-aware evaluator was freshly rerun at the Stage 02
  code candidate, with the locked full corpus: clean 100 cases and dirty 1,100
  cases.
- The ignored attestation binds the report bytes, candidate commit,
  `voice-aware-rerank-v1` evaluator mode, and the locked manifest identities.
  It reports deterministic `PASS`.
- Root@1 58.1897%, Quality@1 61.0453%, Exact@1 13.6853%, boundary precision
  76.5475%, and boundary recall 90.0864% exactly match the Stage 00 safety
  lock; therefore no locked chord-safety metric regressed.

## Gates

| Gate | Result |
| --- | --- |
| Approved fixture identity and Role v1 baseline lock | PASS |
| Deterministic Role v2 shadow evaluation and promotion decision | PASS — `pass-to-stage03` |
| Fresh official chord-safety attestation | PASS — full clean 100 / dirty 1,100 |
| Focused Role / pre-analysis / Stage 02 tests | PASS — 10 files / 56 tests |
| Lint | PASS |
| App TypeScript | PASS |
| Phase docs validation | PASS |
| `git diff --check` | PASS |
| Tracked MIDI / `.local-evaluation` | PASS — 0 / 0 |
| Protected surface and P5.15 audit | PASS — zero protected diff; P5.15 is not an ancestor |

## Protected surfaces

- No P5.15, scoring, boundary, candidate-generation, default analyzer mode,
  default preset, Vault schema/fileVersion, Voicing Memory, or production Role
  v1 behavior changed.
- Voice-internal mixed-note filtering remains deferred to P5.21.1; it was not
  implemented or evaluated as a promotion shortcut.
- No private MIDI, recordings, absolute paths, raw titles, raw notes, or local
  evaluation artifacts are tracked or recorded here.

## Stage commit

- Stage 02 code candidate: `6468cb36bd469d234df763df7873c382757456aa`
  (`feat(P5.21-02): add verified shadow role evaluation`).
- This report and execution-state closure are docs-only and do not change the
  measured code candidate.

## Next action

P5.21-03 is active and may promote the validated candidate, add the required
confidence and review experience, and rerun its own gates. Do not begin
P5.21-04, P5.21.1, P5.22, merge, or push from this Stage 02 closure.