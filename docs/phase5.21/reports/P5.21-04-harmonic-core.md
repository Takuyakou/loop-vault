# P5.21-04-harmonic-core — Harmonic Core Preset

## Determination

`PASS — Harmonic Core is available as an opt-in preset`

Stage 04 is complete at code candidate
`0e8a21bab6ee6de30ba36f97361c6e4ec31a314f` on
`recovery/p521-role-v2-promotion`. This determination applies only to the
separately contracted Harmonic Core preset; release evaluation and human
product acceptance have not run in this stage.

## Findings / Implementation

- Added the opt-in preset named `和声コア`. It gives harmony and pad Voices
  weight `1.3`, excludes bass and percussion, uses `0.8` for mixed, and keeps
  melody at the nonzero safeguard weight `0.15`.
- The preset is not selected by default. Existing presets and custom Voice
  weights retain their prior behavior, and the UI selection remains part of the
  normal pre-analysis flow.
- The tradeoff copy is exactly: `テンションを取りこぼす代わりに、メロディ由来の誤検出を減らします`.
- No mixed-Voice per-note filtering, note-level role weighting, default change,
  scoring change, or storage/schema migration was added.

## Metrics / Gates

| Gate | Result |
| --- | --- |
| Harmonic Core focused tests | PASS — 5 files / 52 tests |
| Adjacent Role v2 / extraction regression | PASS — 6 files / 57 tests |
| App TypeScript (`npx tsc --noEmit`) | PASS |
| E2E TypeScript (`npm run typecheck:e2e`) | PASS |
| Lint (`npm run lint`) | PASS |
| Production build (`npm run build`) | PASS — existing chunk-size advisory only |
| Phase docs validation (`npm run validate:phase-docs`) | PASS |
| `git diff --check` | PASS |
| Test-output hygiene | PASS — no tracked visual, actual, diff, evidence, or Cargo.toml EOL-only output was created |
| Protected surface, privacy, and P5.15 ancestry audit | PASS — no protected/private artifact diff; P5.15 commits are not ancestors |
| Post-gate working tree | PASS — clean |

## Protected surfaces

- The Stage 04 candidate changes only the opt-in Voice-weight preset,
  pre-analysis selection UI, and their focused tests. It does not alter scoring,
  boundaries, candidate generation, default analyzer mode, default preset,
  Vault schema/fileVersion, Voicing Memory, or P5.15.
- No private MIDI, recordings, personal absolute paths, raw titles, raw notes,
  or `.local-evaluation` artifacts are tracked or recorded in this report.
- P5.21.1 mixed-Voice note filtering and P5.22 remain out of scope.

## Stage commit

- Stage 04 code candidate: `0e8a21bab6ee6de30ba36f97361c6e4ec31a314f`
  (`feat(P5.21-04): add opt-in Harmonic Core preset`).
- This report and execution-state update are docs-only closure work and do not
  change the verified code candidate.

## Next action

P5.21-05 is active for full release evaluation and human product acceptance
preparation. Do not start P5.21.1/P5.22, merge, or push from this Stage 04
closure.
