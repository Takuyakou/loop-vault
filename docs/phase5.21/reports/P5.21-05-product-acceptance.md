# P5.21-05-product-acceptance - Product Acceptance

## Final determination

`PASS — Phase 5.21 complete and product accepted`

Automated release gates passed at candidate
`31767832814a009f0f53bb0036df3407d80ca627` on
`recovery/p521-role-v2-promotion`. Human product acceptance was confirmed
on 2026-08-11 after the Harmonic Core result-state and immediate piano-roll
preview improvements. All P5.21 stages are complete. Local master merge is
authorized by the human; push is not performed.

## Findings / Implementation

- Role Evidence v2 remains the promoted production inference from P5.21-03,
  including High/Medium/Low confidence, evidence, review, and the Channel 10
  percussion hard rule. No scoring, boundary, candidate-generation, raw-note,
  default, Vault schema/fileVersion, or Voicing Memory behavior changed in
  this release stage.
- Harmonic Core remains the P5.21-04 opt-in preset. It is not the default;
  existing presets and custom Voice weights retain their behavior.
- The Capture flow now states that a changed contribution mode requires the
  user to run analysis, identifies whether the visible result was produced by
  Standard or Harmonic Core, and reports only anonymous boosted, reduced, and
  excluded Voice counts. It also explains that internal weights can change
  even when visible chord candidates remain the same.
- Selecting Harmonic Core now previews its role contribution immediately in the
  pre-analysis piano roll without claiming that result analysis has run:
  harmony is emphasized, melody is reduced, and bass is omitted from the
  Analysis targets view while remaining dimmed in All Voices. The selected
  preset is still applied to results only by Analyze this configuration.
- The independent Settings visual-hygiene work is integrated as
  `54a8add978dc61d73612a42fdcfd577d515b1cae` and
  `a2655d355f4679b9b424fdd193399c0780a20799`. It supplies deterministic
  build metadata only to the visual-test runner and updates only the approved
  Settings baseline. It makes no production-UI or P5.21 feature change.

## Metrics / Gates

Every required P5.21-05 automated gate below passed against
`31767832814a009f0f53bb0036df3407d80ca627`.

| Gate ID | Result | Recorded result |
| --- | --- | --- |
| `p521-05-phase-docs-validation` | PASS | `npm run validate:phase-docs` |
| `p521-05-role-v2-focused-regression` | PASS | Role v2, pre-analysis, Capture, and result UX focused suite: 13 files / 144 tests |
| `p521-05-role-v2-shadow-evaluation` | PASS | `pass-to-stage03`; exact role accuracy 0.88, manual corrections 3, mixed rate 0.08 |
| `p521-05-official-locked-safety-wrapper` | PASS | Fresh locked official wrapper: clean 100 / dirty 1,100; deterministic attestation bound to this candidate |
| `p521-05-real-midi-no-data-safety` | PASS | Gold / Silver / Bronze records: 0 / 0 / 0; guard failures: 0; no local real-MIDI quality result is claimed |
| `p521-05-midi-benchmark` | PASS | `npm run benchmark:midi` |
| `p521-05-full-vitest` | PASS | 319 files / 2,421 tests |
| `p521-05-playwright-visual-runner` | PASS | 3 / 3 runner tests |
| `p521-05-settings-component` | PASS | 12 / 12 Settings component tests |
| `p521-05-settings-visual` | PASS | 1 / 1; no baseline update during the gate |
| `p521-05-full-playwright` | PASS | 61 / 61; no baseline update during the gate |
| `p521-05-app-typecheck` | PASS | `npx tsc --noEmit` |
| `p521-05-e2e-typecheck` | PASS | `npm run typecheck:e2e` |
| `p521-05-lint` | PASS | `npm run lint` |
| `p521-05-web-production-build` | PASS | `npm run build` |
| `p521-05-tauri-unit-tests` | PASS | `cargo test`: 41 / 41 |
| `p521-05-tauri-release-build` | PASS | `npm run tauri build` |
| `p521-05-diff-check` | PASS | `git diff --check` |
| `p521-05-test-output-hygiene` | PASS | generated Playwright actual/diff/report output remains ignored; no tracked output or Cargo.toml EOL-only diff |
| `p521-05-protected-privacy-p515` | PASS | protected/private artifact audit clean; P5.15 commits are not ancestors |
| `p521-05-post-gate-clean` | PASS | `git status --short` empty after all gates and build |

The generic `npm run eval:midi:voice-aware` command is intentionally a
no-manifest diagnostic in this recovery worktree and reports `ENOENT` because
the ignored local clean/dirty manifests are absent. It is not recorded as a
passing quality evaluation. The contract-specific fresh locked official wrapper
above discovers the registered locked corpus without recording a local path,
binds its attestation to this candidate, and passed.

### Release artifacts

The release build produced these generated, untracked artifacts. Paths are
repository-relative; their sizes and SHA-256 values were verified after the
build.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `src-tauri/target/release/loop-vault.exe` | 22,678,016 | `dc2e179efe11a4c0a1fa86f6e4761452e307f8588b0e477a4c42679aeead7aa4` |
| `src-tauri/target/release/bundle/msi/Loop Vault_1.0.0_x64_en-US.msi` | 12,787,712 | `01dc93bff5a208ff728d601f5443e363c0721911da11dda874d6646b79482834` |
| `src-tauri/target/release/bundle/nsis/Loop Vault_1.0.0_x64-setup.exe` | 11,256,053 | `dd1d66adb39ad7afebcb7b2f9863ded8d92a0009b01fd9e4d6a3a9c81d235a66` |

## Protected surfaces

- No real recordings, private audio, private MIDI, personal absolute paths,
  raw device identifiers, private titles, raw notes, or ignored local
  evaluation inputs are tracked or recorded here. The pre-approved synthetic
  evaluation fixtures remain the only role ground-truth fixture set used by
  this phase.
- No P5.21.1 mixed-Voice note filtering, per-note role weighting, or P5.22
  work was started.
- P5.15 commits `b186281`, `41f50c0`, and `97b03f4` are not ancestors of the
  candidate.
- The independent hygiene work changed only the Settings baseline. This UX
  follow-up additionally changes only
  `e2e/visual.spec.ts-snapshots/capture-analysis-results-chromium-desktop-win32.png`
  through a dedicated one-test baseline-update command. All other baselines
  are unchanged; actual, diff, and evidence output is ignored.
- Post-build checks found no `src-tauri/Cargo.toml` EOL-only diff and no
  generated test output in the working tree.

## Stage commit

The verified code candidate remains
`31767832814a009f0f53bb0036df3407d80ca627`. The source branch final HEAD is
the docs-only human-acceptance closure created from that candidate.

## Next action

Merge `recovery/p521-role-v2-promotion` into clean local `master` with
`--no-ff`. Do not push or start P5.21.1/P5.22.

## Human acceptance

**PASS.** The human confirmed the latest release candidate and immediate
Harmonic Core piano-roll preview, then explicitly authorized the local master
merge on 2026-08-11.

## Merge

Local master merge is authorized and pending this docs-only closure. Push is
not authorized and has not been performed.
